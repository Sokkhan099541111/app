# Python dependencies for the backend.
#
# This file sits in the REPOSITORY ROOT because deployment platforms
# (DigitalOcean App Platform, Render, Railway, Heroku) auto-detect a
# Python application by looking for requirements.txt at the root of the
# source directory. Without it they report "No components detected".
#
# It intentionally mirrors app/requirements.txt. If you add a package,
# add it to BOTH files, or install locally with:
#     python3 -m pip install -r requirements.txt

fastapi
uvicorn[standard]
python-dotenv
requests
sqlalchemy
pymysql
cryptography
reportlab
python-multipart
bcrypt
PyJWT
email-validator
